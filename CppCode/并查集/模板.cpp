/*
???��????????????
1.??????
2.?????????????????????????
*/
#include<iostream>
using namespace std;
int m,n,p[1000];
int chushihua( )  //???????????
{
    for(int i=0;i<1000;i++) p[i] = i;
}
//????，???????，??
int find(int x)
{
    if(p[x]!=x) p[x]=find(p[x]); //??????，???????
    return p[x];
}
// int find(int x)
// {
//     if(p[x]!=x) return p[x]=find(p[x]); //??????，???????
//     else return p[x];
// }
void Union(int a,int b)
{
    p[find(a)]=find(b);//??????????????????????
     /*
    ?????????????
    int a_father=find(a);
    int b_father=find(b);
    p[a_father]=b_father;
    */
}
int main()
{
    return 0;
}